<tr>
  <td>
    <%= image_tag line_item.product.image_url, class: "img-thumbnail", width: "100" if line_item.product.image_url.present? %>
  </td>
  <td><%= line_item.product.name %></td>
  <td><%= line_item.product.description.truncate(50) %></td>
  <td><%= line_item.quantity %></td>
  <td><%= number_to_currency(line_item.product.price) %></td>
  <td><%= number_to_currency(line_item.total_price) %></td>
</tr>